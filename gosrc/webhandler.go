package clubhouse

import (
	// "fmt"
	"net/http"
	// "strings"
)

func get_mime_type(file_name string) string {
	return ""
}

func send_static_page(found *bool, w http.ResponseWriter, r * http.Request) int {
	return 0
}

func handle_gweb_request(found *bool, w http.ResponseWriter, r * http.Request) int {
	return 0
}